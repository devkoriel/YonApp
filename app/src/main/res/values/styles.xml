<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="colorAccent">@color/tab_indicator</item>
        <item name="colorControlNormal">@color/top_bar_background</item>
        <item name="colorControlActivated">@color/top_bar_background</item>
        <item name="colorControlHighlight">@color/top_bar_background</item>
    </style>

    <style name="LibrarySearchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/main_background</item>
        <item name="colorControlActivated">@color/main_background</item>
        <item name="colorControlHighlight">@color/main_background</item>
    </style>

    <style name="TabLayoutStyle" parent="Base.Widget.Design.TabLayout">
        <item name="android:textSize">12dp</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="colorControlNormal">@color/main_background</item>
        <item name="colorControlActivated">@color/main_background</item>
        <item name="colorControlHighlight">@color/main_background</item>
        <item name="android:editTextColor">@color/main_background</item>
        <item name="android:textColorHint">@color/main_background</item>
    </style>

</resources>
